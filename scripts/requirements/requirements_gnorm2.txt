# Requirements for the GNorm2 Metal-GPU conda environment (Python 3.11).
# Install into the gnorm2-tf215 conda env via setup_gnorm2_conda.sh.
#
# tensorflow 2.15 is the last version supported by tensorflow-metal 1.2.0.
# numpy must be < 2.0 for TF 2.15 compatibility.
# transformers >= 4.35 is needed for TFAutoModel; < 4.50 keeps TF backend stable.

tensorflow==2.15.0
tensorflow-metal==1.2.0
numpy>=1.23,<2.0
transformers>=4.35,<4.50
stanza
spacy
click
bioc
