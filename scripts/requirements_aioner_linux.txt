# Requirements for the AIONER conda environment on Linux x86_64.
# Python 3.8 — newest Python supported by TF 2.3.0.
# (Python 3.7 hits cython>=3.1 build failures with modern pip.)
#
# tensorflow==2.3.0 and tensorflow-addons==0.12.1 are installed via conda-forge
# in the startup script (not listed here) because TF 2.3.0 was dropped from PyPI.
# All packages below have native Linux x86_64 wheels on PyPI.

tensorflow-addons==0.12.1
h5py==2.10.0
numpy==1.18.5

transformers==4.18.0
tokenizers==0.12.1
huggingface-hub==0.5.1

stanza==1.4.0
spacy==3.2.4
pydantic>=1.7.4,<1.9.0

bioc==2.0.post4
lxml==4.8.0

tqdm==4.64.0
scipy==1.4.1
torch==1.11.0
